<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/voteicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitalELections"
        tools:targetApi="31">
        <activity
            android:name=".UI.result.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Vote.VoteActivity"
            android:exported="false" />
        <activity
            android:name=".UI.profile.profileActivity"
            android:exported="false" />
        <activity
            android:name=".UI.HomePackage.HomePage"
            android:exported="false" />
        <activity
            android:name=".UI.SignIn.SingInPage"
            android:exported="false" />
        <activity
            android:name=".UI.SignUp.SingUpPage"
            android:exported="false" />
        <activity
            android:name=".UI.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>